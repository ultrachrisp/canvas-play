/**
 * Spinning SVGs is some canvas experiments for chrispyke.com
 *
 * The script will look for an html element with a 'canvas' ID to empty and then run.
 */

import { SpinningSVGs } from "./src/index.ts";

// run the animation
SpinningSVGs();
